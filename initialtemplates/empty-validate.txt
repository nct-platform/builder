# Expected `mrjun.py validate` output for the bundled initialtemplates/empty.mrjun.
# Regenerate with:  mrjun.py unpack initialtemplates/empty.mrjun ./e && mrjun.py validate --project ./e
#
# DIFF AGAINST THIS FILE, do not chase a zero. These findings are the baseline's, not yours:
# every WARNING below and the single ERROR are present before you author a byte. What matters is a NEW
# line appearing after your edit. (The ERROR is locale-conditional: it disappears on a single-locale tenant.)
validate: project

WARNINGS (258):
  ! query 'Countries' references sourceIdentifier b814d61d-84b6-4611-99b3-914e0ca067db not in sources[] (ok if platform-internal)
  ! query 'Rimm Brand Start With' references sourceIdentifier b814d61d-84b6-4611-99b3-914e0ca067db not in sources[] (ok if platform-internal)
  ! query 'Rimm Countries Start with' references sourceIdentifier b814d61d-84b6-4611-99b3-914e0ca067db not in sources[] (ok if platform-internal)
  ! query 'Rimm Equipments' references sourceIdentifier b814d61d-84b6-4611-99b3-914e0ca067db not in sources[] (ok if platform-internal)
  ! query 'Rimm Users by Role Group' references sourceIdentifier b814d61d-84b6-4611-99b3-914e0ca067db not in sources[] (ok if platform-internal)
  ! query 'Rimm User Start With' references sourceIdentifier b814d61d-84b6-4611-99b3-914e0ca067db not in sources[] (ok if platform-internal)
  ! process.table 'Process Table' userStartProcessActions action 'Intake' uses a flat `name` and no `localizedNames` — the UserTask ActionDto ignores `name`, so the button renders BLANK. Use `localizedNames` (a per-locale Map). (05-crud-tree-and-process-table.md)
  ! process.table 'Process Table' globalActions action 'Edit' uses a flat `name` and no `localizedNames` — the UserTask ActionDto ignores `name`, so the button renders BLANK. Use `localizedNames` (a per-locale Map). (05-crud-tree-and-process-table.md)
  ! process.table 'Process Table' userStartProcessActions action 'Create' uses a flat `name` and no `localizedNames` — the UserTask ActionDto ignores `name`, so the button renders BLANK. Use `localizedNames` (a per-locale Map). (05-crud-tree-and-process-table.md)
  ! process.table 'Process Table' globalActions action 'Edit' uses a flat `name` and no `localizedNames` — the UserTask ActionDto ignores `name`, so the button renders BLANK. Use `localizedNames` (a per-locale Map). (05-crud-tree-and-process-table.md)
  ! process.table 'Process Table' columnSettings 'customerName' is scope=CRUD over alias 'ticket' — ProcessTablePlugin.extractColumnValue renders it from contextDataMap[<ctx>].crudDataMap[<alias>] of the case's OWN context data document, and nothing in this export puts that alias there: no start form seeds it, no rule starts a case seeded with it, and a running process files no row of its own. So the column is blank in every row. Seeding it (27-event-driven-process-start.md §4.2) makes it render, but only as the start document's SNAPSHOT — it stops agreeing with the row the moment anything updates it. Prefer scope=GLOBAL over an attribute a serviceTask publishes. (05-crud-tree-and-process-table.md)
  ! process.table 'Process Table' columnSettings 'assignedTechnician' is scope=CRUD over alias 'ticket' — ProcessTablePlugin.extractColumnValue renders it from contextDataMap[<ctx>].crudDataMap[<alias>] of the case's OWN context data document, and nothing in this export puts that alias there: no start form seeds it, no rule starts a case seeded with it, and a running process files no row of its own. So the column is blank in every row. Seeding it (27-event-driven-process-start.md §4.2) makes it render, but only as the start document's SNAPSHOT — it stops agreeing with the row the moment anything updates it. Prefer scope=GLOBAL over an attribute a serviceTask publishes. (05-crud-tree-and-process-table.md)
  ! process.table 'Process Table' indexSettings 'customerName' is scope=CRUD over alias 'ticket' — indexed values are extracted only from a start that CARRIES indexSettings, and again after each action. No start form seeds this alias, and a rule-started case passes no index settings at all, so nothing ever fills this index and every filter over it matches nothing. Index a GLOBAL attribute a serviceTask publishes instead. (05-crud-tree-and-process-table.md, 27-event-driven-process-start.md §6)
  ! process.table 'Process Table' indexSettings 'assignedTechnician' is scope=CRUD over alias 'ticket' — indexed values are extracted only from a start that CARRIES indexSettings, and again after each action. No start form seeds this alias, and a rule-started case passes no index settings at all, so nothing ever fills this index and every filter over it matches nothing. Index a GLOBAL attribute a serviceTask publishes instead. (05-crud-tree-and-process-table.md, 27-event-driven-process-start.md §6)
  ! process.table 'Process Table' columnSettings 'address' is scope=CRUD over alias 'branch' — ProcessTablePlugin.extractColumnValue renders it from contextDataMap[<ctx>].crudDataMap[<alias>] of the case's OWN context data document, and nothing in this export puts that alias there: no start form seeds it, no rule starts a case seeded with it, and a running process files no row of its own. So the column is blank in every row. Seeding it (27-event-driven-process-start.md §4.2) makes it render, but only as the start document's SNAPSHOT — it stops agreeing with the row the moment anything updates it. Prefer scope=GLOBAL over an attribute a serviceTask publishes. (05-crud-tree-and-process-table.md)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'orphan' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'priority' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'estimation' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Business requirement smart analysis' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project precision development tasks' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Projection precision development tasks' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Development planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Project managemen health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.delta' but neither its own html nor anything else on its page contains 'delta' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'numbers' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.numbers text.number' but neither its own html nor anything else on its page contains 'number' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.value-arc path' but neither its own html nor anything else on its page contains 'value-arc' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'main-svg' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'indicatorlayer' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Strategic planning health' js selects '.main-svg .indicatorlayer .trace .title' but neither its own html nor anything else on its page contains 'trace' — the lookup yields undefined and the engine throws ("DOM element provided is null or undefined"). Emit the mount and the selector from ONE helper so they cannot drift. (22)
  ! chart 'Chart Js' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Project managemen health' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Strategic planning health' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'orphan' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'priority' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'estimation' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Business requirement smart analysis' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Project precision development tasks' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Projection precision development tasks' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Development planning health' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Chart Js' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Project managemen health' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! chart 'Strategic planning health' uses Plotly but never sets paper_bgcolor -> renders a WHITE box that clashes with dark themes. Set paper_bgcolor/plot_bgcolor 'rgba(0,0,0,0)' + theme font color (doc 14 §2.2).
  ! page Home (-): child nct.parsis.plugin 'p2' of html 'Card layout' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)
  ! page Home (-): child nct.parsis.plugin 'p4' of html 'Html' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)
  ! page Home (-): child nct.parsis.plugin 'p2' of html 'Card layout' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)
  ! page Home (-): child nct.parsis.plugin 'p3' of html 'Html' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)
  ! page Insights (insights): child nct.parsis.plugin 'p2' of html 'Card layout' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)
  ! page Insights (insights): child nct.parsis.plugin 'p4' of html 'Html' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)
  ! page Insights (insights): child nct.parsis.plugin 'p2' of html 'Card layout' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)
  ! page Insights (insights): child nct.parsis.plugin 'p3' of html 'Html' is never referenced by that html — it is INVISIBLE at runtime (still in the tree, still exported, never drawn). (01-content-model-and-pages.md)

ERRORS (1):
  x left-nav link 'Users' has no item-level localizedMap.name for all 2 locales (got {'en_US': 'Users'}) — the sidebar reads localizedMap.name off the ITEM (PageModel extends LocalizedBean) and falls back to the raw `name`, so this link shows ONE language under every locale. (17-left-nav-quick-links.md / doc 20)

FAIL
